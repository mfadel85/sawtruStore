/** @format */

import { StyleSheet, Dimensions, Platform } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  form: {
    marginTop: 20,
    marginLeft: 10,
    marginRight: 10,
  },
  formContainer: {
    padding: 10,
  },
});
